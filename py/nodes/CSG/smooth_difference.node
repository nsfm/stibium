import fab.types
import fab.shapes

title('Smooth Difference')

input('a', fab.types.Shape)
input('b', fab.types.Shape)
input('m', float, 0.5)
input('o', float, 0)

output('shape', fab.shapes.blend_difference(a, b, m, o))
