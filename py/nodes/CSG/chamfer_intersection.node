import fab.types
import fab.shapes

title('Chamfer Intersection')
input('a', fab.types.Shape)
input('b', fab.types.Shape)
input('r', float, 0.1)

output('shape', fab.shapes.chamfer_intersection(a, b, r))
