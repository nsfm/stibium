import fab

title('Hex prism (Z)')

input('x', float, 0)
input('y', float, 0)
input('z0', float, -1)
input('z1', float, 1)
input('r', float, 1)   # centre-to-vertex (circumradius)

output('shape', fab.shapes.hex_prism_z(x, y, z0, z1, r))

# UI
sb.ui.point(x, y, z0)
sb.ui.point(x, y, z1)
