import fab.types
import fab.shapes

title('Repeat Y (infinite)')

input('shape', fab.types.Shape)
input('spacing', float, 3)
input('y0', float, 0)

output('shape', fab.shapes.repeat_y(shape, spacing, y0))
