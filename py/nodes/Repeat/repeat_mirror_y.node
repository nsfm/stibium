import fab.types
import fab.shapes

title('Repeat Mirror Y')

input('shape', fab.types.Shape)
input('spacing', float, 3)
input('y0', float, 0)

output('shape', fab.shapes.repeat_mirror_y(shape, spacing, y0))
