import fab.types
import fab.shapes

title('Repeat X (infinite)')

input('shape', fab.types.Shape)
input('spacing', float, 3)
input('x0', float, 0)

output('repeated', fab.shapes.repeat_x(shape, spacing, x0))
