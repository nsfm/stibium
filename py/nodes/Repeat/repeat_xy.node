import fab.types
import fab.shapes

title('Repeat Grid XY (infinite)')

input('shape', fab.types.Shape)
input('spacing_x', float, 3)
input('spacing_y', float, 3)
input('x0', float, 0)
input('y0', float, 0)

output('shape', fab.shapes.repeat_xy(shape, spacing_x, spacing_y, x0, y0))
